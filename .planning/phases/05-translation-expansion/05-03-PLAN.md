---
phase: 05-translation-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - scripts/zh-CN.json
autonomous: true
requirements:
  - COV-01
  - COV-02

must_haves:
  truths:
    - "翻译映射表包含 2,500 条及以上翻译条目"
    - "运行 coverage 命令显示总体翻译覆盖率 >= 80%"
    - "长字符串(>20)翻译数量从 826 增长到 2,000+"
    - "所有翻译条目通过 validate 命令质量检查"
  artifacts:
    - path: "scripts/zh-CN.json"
      provides: "翻译映射表，条目数 >= 2,500"
      contains: "translations"
  key_links:
    - from: "scripts/zh-CN.json"
      to: "scripts/i18n/commands/coverage.py"
      via: "coverage 命令验证总覆盖率"
      pattern: "coverage.*80"
    - from: "scripts/zh-CN.json"
      to: "scripts/i18n/commands/validate.py"
      via: "validate 命令验证翻译质量"
      pattern: "validate"
---

<objective>
将 zh-CN.json 翻译条目从约 1,600 条进一步扩充至 2,500+ 条，使总体翻译覆盖率达到 80% 以上。重点填补长字符串(>20 字符)的翻译缺口。

Purpose: 长字符串是覆盖率的主要缺口（当前仅 36.9%），这些长字符串多为用户可见的 UI 提示、错误消息、操作反馈。翻译这些文本能让用户在日常使用中几乎全部看到中文。
Output: 扩充后的 zh-CN.json，条目 >= 2,500，总覆盖率 >= 80%
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-translation-expansion/05-02-SUMMARY.md
@scripts/zh-CN.json
@scripts/i18n/core/scanner.py
@scripts/i18n/core/auto_translate.py
</context>

<interfaces>
<!-- Executor 需要了解的关键接口 -->

From scripts/i18n/core/scanner.py:
```python
_CANDIDATE_RE = re.compile(r'"([A-Z][A-Za-z][^"]{4,120})"')
# 候选字符串：大写开头，至少 6 字符，双引号包裹
# 长字符串(>20) 是主要缺口
```

From scripts/i18n/core/auto_translate.py:
```python
UI_TEMPLATES: List[Tuple[re.Pattern, str]] = [
    (re.compile(r'^Failed to (.+)$'), '{0}失败'),
    (re.compile(r'^Unable to (.+)$'), '无法{0}'),
    # ... 共 22 个模板
]
# UI_TEMPLATES 质量可接受，可用于辅助生成翻译
```

翻译现状（Plan 02 完成后预期）：
- 短字符串(<10): ~91 条已翻译 (95%+)
- 中字符串(10-20): ~384 条已翻译 (100%)
- 长字符串(>20): ~1,125 条已翻译（约 1,600 - 91 - 384）
- 需要再增加约 900 条长字符串翻译达到 2,500 总条目
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 大规模扩充长字符串翻译至 2,500+ 条目</name>
  <files>scripts/zh-CN.json</files>
  <read_first>
    scripts/zh-CN.json
    scripts/i18n/core/auto_translate.py
  </read_first>
  <acceptance_criteria>
    - python3 -c "import json; d=json.load(open('scripts/zh-CN.json')); print(len(d['translations']))" 输出 >= 2500
    - JSON 格式合法（python3 -m json.tool scripts/zh-CN.json > /dev/null 成功）
    - 所有新增翻译值包含中文字符
    - _meta.version 已更新为 "4.4.0"
    - 新增条目中不包含空字符串翻译值
  </acceptance_criteria>
  <action>
  在 Plan 02 的基础上（约 1,600+ 条），继续向 zh-CN.json 添加约 900+ 条长字符串翻译，达到 2,500+ 总条目。

  **翻译策略：**

  1. **利用 UI_TEMPLATES 辅助生成** — auto_translate.py 的 22 个 UI 模板质量可接受，可用于生成框架翻译，然后人工调整
     - "Failed to ..." -> "...失败" 模板可直接使用
     - "Unable to ..." -> "无法..." 模板可直接使用
     - "Please ... to continue" -> "请...以继续" 模板可直接使用

  2. **按类别批量添加** — 将约 900 条新翻译分为以下类别：

  **类别 A: 操作和流程消息 (~250 条)**
  涵盖各种操作反馈、流程状态、进度消息：
  - "The file has been modified" -> "文件已被修改"
  - "Your session has expired" -> "你的会话已过期"
  - "Changes have been applied" -> "更改已应用"
  - "The operation completed successfully" -> "操作成功完成"
  - "Processing complete" -> "处理完成"
  - "Loading configuration" -> "正在加载配置"
  - "Saving changes to disk" -> "正在将更改保存到磁盘"
  - "Initializing workspace" -> "正在初始化工作区"
  - "Synchronizing with remote" -> "正在与远程同步"
  - "Building project index" -> "正在构建项目索引"
  ... （批量添加类似模式的消息翻译）

  **类别 B: 权限和安全消息 (~150 条)**
  涵盖权限请求、安全检查、信任相关：
  - "This action requires permission" -> "此操作需要权限"
  - "Do you trust this file?" -> "你信任此文件吗？"
  - "Allow this tool to run?" -> "允许运行此工具？"
  - "The command was blocked by security policy" -> "命令被安全策略阻止"
  - "Trusted tools can run without confirmation" -> "受信任的工具无需确认即可运行"
  ... （批量添加类似模式的消息翻译）

  **类别 C: 模型和 API 消息 (~150 条)**
  涵盖模型选择、API 交互、Token 使用：
  - "Switching to a different model" -> "切换到不同的模型"
  - "The model is currently unavailable" -> "模型当前不可用"
  - "Usage statistics have been updated" -> "使用统计已更新"
  - "Token count for this conversation" -> "此对话的 Token 数量"
  - "Maximum context length exceeded" -> "已超过最大上下文长度"
  ... （批量添加类似模式的消息翻译）

  **类别 D: Git 和版本控制消息 (~100 条)**
  涵盖 Git 操作、提交、分支管理：
  - "Changes are ready to be committed" -> "更改已准备好提交"
  - "Your branch is up to date" -> "你的分支是最新的"
  - "Merge conflict detected" -> "检测到合并冲突"
  - "The commit was created successfully" -> "提交创建成功"
  - "Push to remote failed" -> "推送到远程失败"
  ... （批量添加类似模式的消息翻译）

  **类别 E: MCP 和工具消息 (~100 条)**
  涵盖 MCP 服务器、工具调用、结果处理：
  - "MCP server is not responding" -> "MCP 服务器未响应"
  - "Tool execution timed out" -> "工具执行超时"
  - "The tool returned an error" -> "工具返回了错误"
  - "No tools are currently available" -> "当前没有可用工具"
  - "Tool configuration has been updated" -> "工具配置已更新"
  ... （批量添加类似模式的消息翻译）

  **类别 F: UI 元素和导航消息 (~150 条)**
  涵盖界面元素、导航、提示信息：
  - "Scroll to see more" -> "滚动查看更多"
  - "Press Enter to continue" -> "按 Enter 继续"
  - "Use arrow keys to navigate" -> "使用方向键导航"
  - "Tab to autocomplete" -> "Tab 键自动补全"
  - "Type your message below" -> "在下方输入你的消息"
  - "Select an option from the list" -> "从列表中选择一个选项"
  - "Drag to resize" -> "拖动调整大小"
  - "Click to expand" -> "点击展开"
  ... （批量添加类似模式的消息翻译）

  **翻译质量要求：**
  - 不使用 prefix_suffix_match 规则
  - 保持格式占位符（%s, {0}, ${var}）不变
  - 不翻译代码标识符、变量名、命令名、URL
  - 技术术语保持英文（如 "Token", "API", "MCP", "JSON"）
  - 翻译自然流畅，符合中文表达习惯
  - 避免逐字翻译，注重语义传达

  **实现方式：**
  1. 读取 Plan 02 完成后的 zh-CN.json
  2. 批量添加约 900 条新翻译到 translations 字典
  3. 更新 _meta.version 为 "4.4.0"
  4. 按 key 字母排序确保 JSON 有序
  5. 使用 json.dumps(ensure_ascii=False, indent=2) 写回
  6. 验证 JSON 合法性和条目数 >= 2,500
  </action>
  <verify>
    <automated>cd /Users/zhaoluolu/Projects/claude-code-i18n && python3 -c "import json; d=json.load(open('scripts/zh-CN.json')); t=d['translations']; print(f'条目数: {len(t)}'); assert len(t) >= 2500, f'条目数不足: {len(t)}'; assert d['_meta']['version'] == '4.4.0'; empty=[k for k,v in t.items() if not v]; assert not empty, f'空翻译: {empty[:5]}'; print('验证通过')" && python3 -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - zh-CN.json 包含 2,500+ 条翻译条目
    - 所有翻译值为非空中文字符串
    - JSON 格式合法
    - _meta.version 更新为 4.4.0
    - 现有测试套件通过
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; d=json.load(open('scripts/zh-CN.json')); print(len(d['translations']))"` 显示 2500+
2. `python3 -m json.tool scripts/zh-CN.json > /dev/null` JSON 格式合法
3. `python3 -m pytest tests/ -x -q` 现有测试通过
</verification>

<success_criteria>
- zh-CN.json 翻译条目 >= 2,500
- JSON 格式合法
- 翻译质量经过人工策展，自然准确
</success_criteria>

<output>
完成后创建 `.planning/phases/05-translation-expansion/05-03-SUMMARY.md`
</output>
