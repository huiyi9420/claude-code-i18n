---
phase: 01-foundation-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/i18n/__init__.py
  - scripts/i18n/config/__init__.py
  - scripts/i18n/config/constants.py
  - scripts/i18n/config/paths.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/unit/__init__.py
  - tests/unit/test_paths.py
  - tests/fixtures/sample_cli.js
  - tests/fixtures/sample_package.json
autonomous: true
requirements:
  - PATH-01
  - PATH-02
  - PATH-03
  - PATH-04
  - PATH-05

must_haves:
  truths:
    - "引擎通过 shutil.which + volta which + npm root + 常见路径级联自动检测到 Claude Code CLI 安装路径"
    - "用户可通过 CLAUDE_I18N_CLI_DIR 环境变量覆盖自动检测结果"
    - "用户可通过 ~/.claude/i18n.json 配置文件覆盖自动检测结果"
    - "路径验证确认目标目录包含 cli.js 和 package.json 且包名为 @anthropic-ai/claude-code"
    - "CLI 未安装时用户看到清晰的错误信息和安装指引（非 Python traceback）"
  artifacts:
    - path: "scripts/i18n/config/paths.py"
      provides: "5 级级联路径检测 + 路径验证"
      exports: ["find_cli_install_dir", "validate_cli_dir"]
    - path: "scripts/i18n/config/constants.py"
      provides: "包名、备份文件名、哈希文件名等常量"
      exports: ["PKG_NAME", "BACKUP_NAME", "HASH_NAME"]
    - path: "tests/unit/test_paths.py"
      provides: "PATH-01 ~ PATH-05 全覆盖的单元测试"
      min_lines: 80
  key_links:
    - from: "scripts/i18n/config/paths.py"
      to: "shutil.which / subprocess.run / json.load"
      via: "5 级级联检测函数"
      pattern: "find_cli_install_dir"
    - from: "tests/unit/test_paths.py"
      to: "scripts/i18n/config/paths.py"
      via: "import + 函数调用"
      pattern: "from.*paths import.*find_cli_install_dir"
---

<objective>
创建 Claude Code CLI 安装路径自动检测系统。

Purpose: 解决 v3.0 硬编码路径的问题，使引擎在任何安装方式（Volta/npm global/common paths）下都能正确定位 cli.js。路径检测是所有后续功能的基础。
Output: paths.py (5 级级联检测) + constants.py + 完整的单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-safety/01-RESEARCH.md

<interfaces>
<!-- 当前 v3.0 硬编码路径（需要替换的对象） -->
<!-- 来自 scripts/localize.py lines 17-23: -->
```python
CLI_DIR   = Path("/Users/zhaolulu/.volta/tools/image/packages/@anthropic-ai/claude-code/lib/node_modules/@anthropic-ai/claude-code")
CLI_FILE  = CLI_DIR / "cli.js"
BACKUP    = CLI_DIR / "cli.bak.en.js"
PKG_JSON  = CLI_DIR / "package.json"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建测试基础设施 + 路径解析器常量</name>
  <files>
    tests/__init__.py,
    tests/conftest.py,
    tests/unit/__init__.py,
    tests/unit/test_paths.py,
    tests/fixtures/sample_cli.js,
    tests/fixtures/sample_package.json,
    scripts/i18n/__init__.py,
    scripts/i18n/config/__init__.py,
    scripts/i18n/config/constants.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-safety/01-RESEARCH.md (Pattern 1: 多策略级联路径检测 + Code Examples)
  </read_first>
  <behavior>
    - Test env_var_override: 设置 CLAUDE_I18N_CLI_DIR 环境变量指向有效目录，find_cli_install_dir 返回 (path, 'env_var')
    - Test config_file_override: 创建 ~/.claude/i18n.json 含 cli_path，find_cli_install_dir 返回 (path, 'config_file')
    - Test volta_detection: mock shutil.which('volta') 返回 volta 路径，mock subprocess.run 返回 volta which claude 结果
    - Test npm_global_detection: mock npm root -g 返回有效 npm 全局目录
    - Test common_path_fallback: 所有 mock 策略失败时，在常见路径找到有效安装
    - Test not_found_error: 所有策略失败，find_cli_install_dir 返回 (None, 'not_found')
    - Test validate_cli_dir_valid: 有效目录（含 cli.js + package.json + 正确 name 字段）返回 True
    - Test validate_cli_dir_invalid: 缺少文件或 name 不匹配返回 False
    - Test validate_cli_dir_with_cjk_cli: cli.js 含中文字符时 validate 仍然返回 True（验证只检查结构和包名，不检查内容纯净性）
  </behavior>
  <action>
    1. 创建目录结构: scripts/i18n/, scripts/i18n/config/, tests/, tests/unit/, tests/fixtures/

    2. 创建 tests/fixtures/sample_package.json:
       ```json
       {"name": "@anthropic-ai/claude-code", "version": "2.1.92"}
       ```

    3. 创建 tests/fixtures/sample_cli.js:
       一个 ~1KB 的合法 JS 文件，包含多种引号内的英文字符串（用于后续测试）。
       内容示例: `var e={title:"Claude Code",msg:"Permission denied",short:"OK"};function run(){console.log("Running")}`

    4. 创建 tests/conftest.py:
       - 提供 mock_cli_dir fixture: 创建临时目录，放入 sample_cli.js 和 sample_package.json，返回目录 Path
       - 提供 mock_volta fixture: mock subprocess.run 返回 volta which claude 的模拟输出
       - 提供 mock_npm_root fixture: mock subprocess.run 返回 npm root -g 的模拟输出
       - 提供 clean_env fixture: 确保 CLAUDE_I18N_CLI_DIR 环境变量被清理

    5. 创建 scripts/i18n/__init__.py 和 scripts/i18n/config/__init__.py (空文件)

    6. 创建 scripts/i18n/config/constants.py:
       ```python
       PKG_NAME = '@anthropic-ai/claude-code'
       BACKUP_NAME = 'cli.bak.en.js'
       HASH_NAME = 'cli.backup.hash'
       ```

    7. 创建 tests/unit/test_paths.py -- 编写上述 9 个测试用例。
       测试应能导入 paths 模块（即使模块尚未实现，测试文件先写好，预期 import 失败是 RED 状态）。

    注意：Python 版本兼容性 -- 不使用 3.10+ 语法（match/case），不使用 3.12+ 语法。
    使用 `from typing import Optional, Tuple` 做类型注解。
  </action>
  <verify>
    <automated>cd /Users/zhaolulu/Projects/claude-code-i18n && python -m pytest tests/unit/test_paths.py -x --tb=short 2>&1 | head -30</automated>
    预期: 测试文件存在但 paths.py 不存在，所以 import 失败是 RED 状态
  </verify>
  <done>
    - tests/ 目录结构完整（__init__.py、conftest.py、fixtures）
    - test_paths.py 包含 9 个测试用例覆盖 PATH-01 ~ PATH-05
    - constants.py 包含 PKG_NAME、BACKUP_NAME、HASH_NAME 常量
    - 测试处于 RED 状态（paths.py 尚未实现）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Path Resolver</name>
  <files>scripts/i18n/config/paths.py</files>
  <read_first>
    - .planning/phases/01-foundation-safety/01-RESEARCH.md (Pattern 1 完整代码示例 + Pitfall 1: Volta Shim 误判)
    - tests/unit/test_paths.py (当前测试用例)
  </read_first>
  <acceptance_criteria>
    - paths.py 导出 find_cli_install_dir() 函数，签名: () -> Tuple[Optional[Path], str]
    - paths.py 导出 validate_cli_dir() 函数，签名: (Path) -> bool
    - find_cli_install_dir 按优先级尝试 5 种策略: env_var -> config_file -> volta -> npm_global -> common_path
    - validate_cli_dir 检查: is_dir() + cli.js exists + package.json exists + package.json name == PKG_NAME
    - 所有 9 个测试用例通过 (pytest tests/unit/test_paths.py -x 全绿)
    - 无外部依赖导入（只用标准库）
  </acceptance_criteria>
  <action>
    实现 scripts/i18n/config/paths.py，参考 01-RESEARCH.md Pattern 1 代码示例。

    关键实现细节:
    1. find_cli_install_dir() 返回 (Optional[Path], str)，第二个元素是检测方法名
    2. 5 级级联检测按优先级: env_var > config_file > volta > npm_global > common_path
    3. validate_cli_dir() 检查目标目录有效性:
       - p.is_dir() 为 True
       - p / 'cli.js' 存在
       - p / 'package.json' 存在且 json.load 后 name == '@anthropic-ai/claude-code'
    4. Volta 策略特殊处理 (Pitfall 1):
       - 先检测 shutil.which('volta') 是否存在
       - 调用 subprocess.run(['volta', 'which', 'claude'], capture_output=True, text=True, timeout=5)
       - 对输出路径调用 Path.resolve() 获取真实 cli.js 路径
    5. npm global 策略:
       - subprocess.run(['npm', 'root', '-g'], capture_output=True, text=True, timeout=5)
       - 拼接 PKG_NAME 形成完整路径
    6. common_path 策略枚举:
       - ~/.volta/tools/image/packages/{PKG_NAME}/lib/node_modules/{PKG_NAME}
       - /usr/local/lib/node_modules/{PKG_NAME}
       - /opt/homebrew/lib/node_modules/{PKG_NAME}
    7. 所有 subprocess 调用用 try/except 包裹 (TimeoutExpired, OSError)
    8. 所有路径操作使用 pathlib.Path，不用 os.path

    实现完成后运行测试确认 GREEN 状态。
  </action>
  <verify>
    <automated>cd /Users/zhaolulu/Projects/claude-code-i18n && python -m pytest tests/unit/test_paths.py -v</automated>
  </verify>
  <done>
    - paths.py 导出 find_cli_install_dir 和 validate_cli_dir
    - 5 级级联检测完整实现
    - Volta shim 特殊处理正确
    - 所有 9 个测试 GREEN
    - 零外部依赖
  </done>
</task>

</tasks>

<verification>
1. 单元测试全通过: `python -m pytest tests/unit/test_paths.py -v`
2. 模块可导入: `python -c "from scripts.i18n.config.paths import find_cli_install_dir"`
3. 常量可导入: `python -c "from scripts.i18n.config.constants import PKG_NAME"`
</verification>

<success_criteria>
- PATH-01: shutil.which + volta which + Path.resolve() 级联检测 -- 测试覆盖
- PATH-02: CLAUDE_I18N_CLI_DIR 环境变量覆盖 -- 测试覆盖
- PATH-03: ~/.claude/i18n.json 配置文件覆盖 -- 测试覆盖
- PATH-04: validate_cli_dir 检查 cli.js + package.json + name -- 测试覆盖
- PATH-05: (None, 'not_found') 返回 + 调用方可构建清晰错误信息 -- 测试覆盖
- 所有测试 GREEN，零外部依赖
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-safety/01-01-SUMMARY.md`
</output>
