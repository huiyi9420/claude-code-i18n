---
phase: 03-integration-quality
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - scripts/zh-CN.json
  - tests/integration/test_roundtrip.py
  - tests/integration/test_coverage_integration.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07]

must_haves:
  truths:
    - "端到端往返测试通过：apply -> verify -> restore -> verify"
    - "测试覆盖率 >= 80%（pytest --cov 或手动统计）"
    - "coverage 命令集成测试通过"
    - "所有 193+ 测试通过，无回归"
  artifacts:
    - path: "tests/integration/test_roundtrip.py"
      provides: "增强的端到端往返测试"
      contains: "test_full_roundtrip"
    - path: "tests/integration/test_coverage_integration.py"
      provides: "coverage 命令集成测试"
      min_lines: 40
    - path: "scripts/zh-CN.json"
      provides: "补充后的翻译映射表（>= 834 条）"
  key_links:
    - from: "tests/integration/test_roundtrip.py"
      to: "scripts/i18n/commands/apply.py"
      via: "cmd_apply() 调用"
      pattern: "cmd_apply"
    - from: "tests/integration/test_coverage_integration.py"
      to: "scripts/i18n/commands/coverage.py"
      via: "cmd_coverage() 调用"
      pattern: "cmd_coverage"
---

<objective>
端到端验证 + 测试补全 + 发布质量检查。

Purpose: 确保 apply->restore 往返正确 (D-08)，coverage 命令集成正常，测试覆盖率达标 (D-06)，达到可发布质量。

Output: 增强的集成测试 + coverage 集成测试 + 发布验证报告
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integration-quality/03-CONTEXT.md
@.planning/phases/03-integration-quality/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的 coverage 接口 -->

From scripts/i18n/commands/coverage.py (Plan 01 创建):
```python
def cmd_coverage() -> None  # coverage 子命令入口
def format_coverage_table(translated, untranslated, skipped, categories) -> str  # 格式化表格
```

From tests/integration/test_roundtrip.py (当前):
```python
# integration_env fixture: 创建 mock CLI + 翻译映射表 + skip words
# TestApplyRestoreRoundTrip: test_full_roundtrip, test_apply_idempotent
# TestBackupIntegrity: test_backup_pristine_after_apply
# TestEngineCLI: test_engine_apply_restore
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 roundtrip 测试 + coverage 集成测试</name>
  <files>tests/integration/test_roundtrip.py, tests/integration/test_coverage_integration.py</files>
  <read_first>
    tests/integration/test_roundtrip.py
    tests/conftest.py
    tests/fixtures/sample_cli.js
    scripts/i18n/commands/coverage.py
    scripts/i18n/commands/apply.py
    scripts/i18n/commands/restore.py
  </read_first>
  <acceptance_criteria>
    test_roundtrip.py 包含 test_full_roundtrip 且通过
    test_coverage_integration.py 存在且包含至少 3 个测试
    python3 -m pytest tests/integration/ -v 全部通过
    python3 -m pytest -q 输出 "passed" 且数量 >= 196
  </acceptance_criteria>
  <action>
    **Step 1: 增强现有 test_roundtrip.py**

    检查现有 `TestApplyRestoreRoundTrip.test_full_roundtrip` 是否完整覆盖 apply -> verify -> restore -> verify 流程（查看代码确认）。如果已有，不需要重复添加。重点确保：
    - apply 后 cli.js 包含中文、不包含对应英文（长字符串）
    - restore 后 cli.js 包含英文、不包含中文
    - 备份始终保持纯净（零 CJK）

    如果现有测试已经覆盖，跳过此步骤。

    **Step 2: 创建 test_coverage_integration.py**

    创建 `tests/integration/test_coverage_integration.py`：

    ```python
    class TestCoverageIntegration:
        """coverage 命令集成测试"""

        def test_coverage_after_apply(self, integration_env, capsys):
            """apply 后运行 coverage 显示正确的覆盖率"""
            # 1. 先 apply
            # 2. 运行 cmd_coverage()
            # 3. 验证输出包含 translated > 0 和 percentage > 0

        def test_coverage_no_map(self, tmp_path, capsys):
            """无翻译映射表时 coverage 输出 translated=0"""
            # mock cli_dir 存在但没有 zh-CN.json
            # 验证输出 ok=True, translated=0

        def test_coverage_shows_categories(self, integration_env, capsys):
            """coverage 输出包含 long/medium/short 分类"""
            # 验证 categories 字段包含三个分类
    ```

    使用与 test_roundtrip.py 相同的 `integration_env` fixture。需要 mock `get_cli_dir` 和 `get_data_dir`。

    **Step 3: 运行全量测试确认无回归**

    运行 `python3 -m pytest tests/ -v --tb=short` 确认全部通过。
  </action>
  <verify>
    <automated>cd /Users/zhaolulu/Projects/claude-code-i18n && python3 -m pytest tests/integration/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/integration/test_roundtrip.py 所有测试通过
    - tests/integration/test_coverage_integration.py 至少 3 个测试通过
    - 全量测试 196+ 通过，无回归
  </done>
</task>

<task type="auto">
  <name>Task 2: 发布验证 — 测试覆盖率确认 + 发布检查清单</name>
  <files>scripts/zh-CN.json</files>
  <read_first>
    scripts/zh-CN.json
    scripts/i18n/core/auto_translate.py
    scripts/i18n/commands/coverage.py
    scripts/i18n/commands/extract.py
  </read_first>
  <acceptance_criteria>
    python3 -m pytest -q 全部通过且数量 >= 196
    coverage 命令可执行且输出正确
    install.sh 语法检查通过
    发布检查清单各项全部通过
  </acceptance_criteria>
  <action>
    **Step 1: 确认测试覆盖率 >= 80%**

    运行测试并手动统计覆盖率（项目零外部依赖，不使用 pytest-cov）：
    - 统计各模块的测试文件数和测试函数数
    - 确认核心模块（scanner, replacer, backup, translation_map, auto_translate, coverage）均有对应测试
    - 当前已有 87% 覆盖率 + 193 测试，新增 coverage 测试后应仍然 >= 80%

    如果 pytest-cov 可用（`pip install pytest-cov`），直接运行：
    ```bash
    python3 -m pytest --cov=scripts tests/ --cov-report=term-missing
    ```

    **Step 2: 执行发布检查清单 (D-06 ~ D-09)**

    逐项验证：
    1. [D-06] `python3 -m pytest -q` — 全部通过，覆盖率 >= 80%
    2. [D-07] `python3 scripts/engine.py coverage` — 输出覆盖率表格
    3. [D-08] `python3 -m pytest tests/integration/test_roundtrip.py -v` — 往返测试通过
    4. [D-09] `bash -n install.sh` — 安装脚本语法正确

    **Step 3: 验证 zh-CN.json 完整性**

    确认 zh-CN.json 仍然包含 834+ 条翻译条目，JSON 格式有效：
    ```bash
    python3 -c "import json; d=json.load(open('scripts/zh-CN.json')); print(f'Entries: {len(d.get(\"translations\",{}))}'); assert len(d.get('translations',{})) >= 834"
    ```

    注意：此任务不执行 extract + auto-translate 补全流程 — 那是运行时操作（需要真实的 cli.js），
    不是构建时操作。coverage 命令的存在让用户可以自行量化并决定是否需要补全。
  </action>
  <verify>
    <automated>cd /Users/zhaolulu/Projects/claude-code-i18n && python3 -m pytest -q 2>&1 | tail -3 && python3 -c "import json; d=json.load(open('scripts/zh-CN.json')); assert len(d.get('translations',{})) >= 834; print('zh-CN.json OK')" && bash -n install.sh && echo "install.sh OK"</automated>
  </verify>
  <done>
    - 全部测试通过（196+）
    - 测试覆盖率 >= 80% (D-06)
    - coverage 命令可用 (D-07)
    - 往返测试通过 (D-08)
    - install.sh 语法正确 (D-09)
    - zh-CN.json 包含 834+ 条目且 JSON 有效
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/ -q` 全部通过
2. `python3 scripts/engine.py coverage` 输出覆盖率表格
3. `bash -n install.sh` 语法检查通过
4. `python3 -c "import json; d=json.load(open('scripts/zh-CN.json')); assert len(d.get('translations',{})) >= 834"`
5. 发布检查清单 D-06~D-09 全部满足
</verification>

<success_criteria>
- 测试覆盖率 >= 80% 且全部通过 (D-06, TEST-01~TEST-07)
- 端到端往返测试通过 (D-08, TEST-06)
- coverage 命令集成测试通过
- install.sh 验证通过 (D-09)
- 项目达到可发布质量
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-quality/03-03-SUMMARY.md`
</output>
