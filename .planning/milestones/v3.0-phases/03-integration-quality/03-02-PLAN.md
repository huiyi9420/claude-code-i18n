---
phase: 03-integration-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - commands/auto-i18n.md
  - .github/workflows/ci.yml
  - tests/test_install.sh
autonomous: true
requirements: [INST-01, INST-02, INST-03]

must_haves:
  truths:
    - "install.sh 在干净环境执行后，所有组件安装到 ~/.claude/ 目录"
    - "install.sh 安装前检查 Python 3 和 Node.js 可用性，缺失时报错退出"
    - "技能命令 auto-i18n.md 包含 coverage 子命令的使用说明"
    - "CI workflow 在 push/PR 时自动运行测试"
  artifacts:
    - path: "install.sh"
      provides: "增强版安装脚本，包含 coverage 模块安装 + 环境检查 + 完整验证"
      min_lines: 100
    - path: "commands/auto-i18n.md"
      provides: "同步的技能命令描述，包含 coverage"
      contains: "coverage"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow"
      contains: "pytest"
    - path: "tests/test_install.sh"
      provides: "安装脚本测试"
      min_lines: 30
  key_links:
    - from: "install.sh"
      to: "~/.claude/scripts/i18n/"
      via: "cp 命令复制引擎模块"
      pattern: "cp.*scripts/i18n/commands/coverage\.py"
    - from: ".github/workflows/ci.yml"
      to: "tests/"
      via: "pytest 运行测试套件"
      pattern: "python.*pytest"
---

<objective>
增强安装脚本 + 更新技能命令 + 搭建 CI 流水线。

Purpose: 确保安装脚本包含 coverage 等新增模块 (INST-01)，技能命令与引擎能力同步 (INST-02)，环境检查完整 (INST-03)，CI 自动化回归检测 (D-05)。

Output: 增强版 install.sh + 更新的 auto-i18n.md + GitHub Actions CI + 安装脚本测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integration-quality/03-CONTEXT.md

<interfaces>
<!-- install.sh 当前结构和需要更新的位置 -->

From install.sh (当前):
- TARGET_DIR="${HOME}/.claude/scripts/i18n"
- 引擎模块复制列表在 "# Copy engine modules" 下
- 命令模块列表: apply.py, extract.py, status.py, restore.py, auto_update.py
- 验证文件列表在 "[4/4] 验证安装" 下
- 缺少 coverage.py 的安装

From commands/auto-i18n.md (当前):
- 参数映射表只有: 无/apply, restore, status, auto-update
- 缺少 coverage 参数
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 install.sh + auto-i18n.md</name>
  <files>install.sh, commands/auto-i18n.md, tests/test_install.sh</files>
  <read_first>
    install.sh
    commands/auto-i18n.md
  </read_first>
  <acceptance_criteria>
    install.sh 中包含 "coverage.py" 的 cp 和验证行
    auto-i18n.md 中包含 "coverage" 的参数映射
    tests/test_install.sh 可执行且通过
  </acceptance_criteria>
  <action>
    **Step 1: 更新 install.sh**

    1. 在命令模块复制部分（第 91 行附近），`for f in __init__.py apply.py extract.py status.py restore.py auto_update.py; do` 改为:
       ```bash
       for f in __init__.py apply.py extract.py status.py restore.py auto_update.py coverage.py; do
       ```

    2. 在验证文件列表中（第 139 行附近的 MISSING 循环），添加:
       ```bash
       "${TARGET_DIR}/commands/coverage.py" \
       ```

    3. 添加覆盖命令安装后的提示（可选，在验证部分之后）:
       无需额外提示，现有 "使用方法" 部分已足够。

    **Step 2: 更新 commands/auto-i18n.md**

    1. 在参数映射表中添加 coverage 行:
       ```markdown
       | coverage | 查看翻译覆盖率报告 |
       ```

    2. 在 "---" 分隔线后添加 coverage 流程说明:
       ```markdown
       ### coverage 流程

       ```bash
       python3 ~/.claude/scripts/engine.py coverage
       ```

       输出翻译覆盖率表格，显示已翻译/未翻译/跳过的条目数量和百分比。
       ```

    **Step 3: 创建 tests/test_install.sh**

    创建安装脚本的轻量测试：
    - 验证 install.sh 语法正确（bash -n install.sh）
    - 验证所有 cp 的源文件存在
    - 验证关键变量定义（TARGET_DIR, COMMAND_FILE 等）
    - 使用临时目录作为 DESTDIR 模拟安装（如果可行）
    - 不真正安装到 ~/.claude/（避免污染真实环境）
  </action>
  <verify>
    <automated>cd /Users/zhaolulu/Projects/claude-code-i18n && bash -n install.sh && echo "install.sh syntax OK" && bash tests/test_install.sh && grep -q "coverage" commands/auto-i18n.md && echo "auto-i18n.md has coverage"</automated>
  </verify>
  <done>
    - install.sh 包含 coverage.py 的安装和验证
    - install.sh 通过 bash -n 语法检查
    - auto-i18n.md 包含 coverage 参数映射和使用说明
    - tests/test_install.sh 验证安装脚本基本正确性
  </done>
</task>

<task type="auto">
  <name>Task 2: 搭建 GitHub Actions CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    tests/conftest.py
    tests/unit/test_coverage_cmd.py
  </read_first>
  <acceptance_criteria>
    .github/workflows/ci.yml 存在且包含 pytest 步骤
    CI workflow 在 push 到 main 和 PR 时触发
    包含 Python 3.8+ 和 Node.js 的环境设置
  </acceptance_criteria>
  <action>
    创建 `.github/workflows/ci.yml`：

    ```yaml
    name: CI
    on:
      push:
        branches: [main, master]
      pull_request:
        branches: [main, master]

    jobs:
      test:
        runs-on: ubuntu-latest
        strategy:
          matrix:
            python-version: ['3.10', '3.12', '3.14']

        steps:
          - uses: actions/checkout@v4

          - name: Set up Python ${{ matrix.python-version }}
            uses: actions/setup-python@v5
            with:
              python-version: ${{ matrix.python-version }}

          - name: Set up Node.js
            uses: actions/setup-node@v4
            with:
              node-version: '22'

          - name: Install pytest
            run: pip install pytest

          - name: Run tests
            run: python3 -m pytest tests/ -v --tb=short

          - name: Check install script syntax
            run: bash -n install.sh
    ```

    注意：
    - 不使用覆盖率工具（避免外部依赖），仅验证测试通过
    - Python 3.10 是项目最低兼容版本的合理测试下限（3.8 在 ubuntu-latest 上可能不可用）
    - Node.js 22 用于语法验证测试
    - install.sh 语法检查作为额外保障
  </action>
  <verify>
    <automated>cd /Users/zhaolulu/Projects/claude-code-i18n && test -f .github/workflows/ci.yml && grep -q "pytest" .github/workflows/ci.yml && echo "CI workflow created"</automated>
  </verify>
  <done>
    - .github/workflows/ci.yml 存在
    - workflow 在 push/PR 时触发
    - 包含 Python 多版本矩阵测试
    - 包含 pytest 和 install.sh 语法检查步骤
  </done>
</task>

</tasks>

<verification>
1. `bash -n install.sh` 语法通过
2. `grep "coverage.py" install.sh` 确认 coverage 模块被安装
3. `grep "coverage" commands/auto-i18n.md` 确认技能命令同步
4. `test -f .github/workflows/ci.yml` CI 文件存在
5. `bash tests/test_install.sh` 安装脚本测试通过
</verification>

<success_criteria>
- install.sh 包含 coverage.py 安装和验证 (INST-01)
- auto-i18n.md 与引擎能力一致 (INST-02)
- install.sh 检查 Python 3 + Node.js 环境可用性 (INST-03)
- GitHub Actions CI workflow 在 push/PR 时运行测试 (D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-quality/03-02-SUMMARY.md`
</output>
