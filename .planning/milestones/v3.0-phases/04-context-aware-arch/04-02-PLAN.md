---
phase: 04-context-aware-arch
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/i18n/core/replacer.py
  - scripts/i18n/commands/apply.py
  - tests/unit/test_replacer.py
autonomous: true
requirements: [CTX-02]

must_haves:
  truths:
    - "apply 命令根据 cli.js 中字符串位置选择上下文翻译"
    - "精确匹配的上下文翻译优先于全局默认翻译"
    - "无上下文标签的翻译条目行为不变（向后兼容）"
  artifacts:
    - path: "scripts/i18n/core/replacer.py"
      provides: "上下文感知替换引擎"
      exports: ["apply_translations"]
    - path: "scripts/i18n/commands/apply.py"
      provides: "上下文感知 apply 命令流程"
    - path: "tests/unit/test_replacer.py"
      provides: "上下文感知替换测试"
  key_links:
    - from: "scripts/i18n/commands/apply.py"
      to: "scripts/i18n/core/context_detector.py"
      via: "import build_context_index, detect_context"
      pattern: "build_context_index|detect_context"
    - from: "scripts/i18n/core/replacer.py"
      to: "scripts/i18n/io/translation_map.py"
      via: "import resolve_translation"
      pattern: "resolve_translation"
---

<objective>
上下文感知替换引擎

Purpose: 让 apply 命令在替换字符串时，根据字符串在 cli.js 中的位置上下文，优先选择精确匹配的上下文翻译。无精确匹配时回退到全局默认翻译。现有无上下文标签的翻译条目行为完全不变。

Output: 修改后的 replacer.py（上下文感知），修改后的 apply.py（集成上下文检测），完整的单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-context-aware-arch/04-CONTEXT.md
@.planning/phases/04-context-aware-arch/04-01-SUMMARY.md
@scripts/i18n/core/replacer.py
@scripts/i18n/commands/apply.py

<interfaces>
<!-- Plan 01 创建的接口，executor 必须使用 -->

From scripts/i18n/io/translation_map.py (Plan 01 扩展后):
```python
def load_translation_map(map_path: Path) -> dict:
    # 返回 {"meta": ..., "translations": {en: zh}, "raw_translations": {en: raw_value}}
    # translations: 展平的 {en: zh}（向后兼容）
    # raw_translations: 原始结构（可能是 str、{"zh": zh}、{"zh": zh, "contexts": {...}}）

def resolve_translation(entry, context_tags: list) -> str:
    # 给定翻译条目（str/dict）和上下文标签列表，返回最匹配的中文翻译
    # 精确上下文 > 全局默认
```

From scripts/i18n/core/context_detector.py (Plan 01 创建):
```python
def build_context_index(content: str) -> list[dict]:
    # 返回 [{"tag": "tools", "start": 12345, "end": 23456}, ...]
    # 一次性扫描 cli.js 内容，构建位置到上下文的映射索引

def detect_context(index: list, position: int) -> list[str]:
    # 给定预构建的 index 和字符串位置，返回上下文标签列表
    # 无匹配返回 ["default"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 上下文感知替换引擎</name>
  <files>scripts/i18n/core/replacer.py, scripts/i18n/commands/apply.py, tests/unit/test_replacer.py</files>
  <read_first>scripts/i18n/core/replacer.py, scripts/i18n/commands/apply.py, tests/unit/test_replacer.py</read_first>
  <behavior>
    - apply_translations 接收新的可选参数 raw_translations 和 context_index
    - 替换时：先用 context_index 检测匹配位置的上下文标签，再调用 resolve_translation 选择翻译
    - 无上下文信息的翻译条目（v4 字符串）行为完全不变
    - 有上下文信息但无匹配上下文的条目回退到全局默认翻译
    - 替换统计中新增 "contextual" 计数器
    - apply 命令流程集成：build_context_index -> apply_translations（传入 index）
  </behavior>
  <action>
    先编写测试（RED），然后实现（GREEN）：

    1. 在 test_replacer.py 中新增测试：
       - test_apply_with_context_exact_match: 包含上下文翻译的条目，在对应区域替换为上下文翻译
       - test_apply_with_context_fallback: 包含上下文翻译的条目，在非匹配区域替换为全局默认
       - test_apply_without_context_index: 不传 context_index 时，行为与当前完全一致（向后兼容）
       - test_apply_mixed_context_and_plain: 混合有上下文和无上下文的翻译条目
       - test_contextual_stats: 返回的 stats 中包含 "contextual" 计数
       - test_apply_no_raw_translations: 不传 raw_translations 时使用 translations 展平字典

    2. 修改 replacer.py 中的 apply_translations 函数签名：
       ```python
       def apply_translations(
           content: str,
           translations: Dict[str, str],
           skip_words: Set[str],
           max_short_cap: int = 50,
           raw_translations: Dict = None,      # 新增：原始翻译结构
           context_index: List[dict] = None,    # 新增：上下文索引
       ) -> Tuple[str, dict]:
       ```

       修改替换逻辑：
       - 在三级分类的每个匹配循环中，对每个匹配位置调用 detect_context(context_index, match.start()) 获取上下文标签
       - 如果 raw_translations 中该英文有 contexts，调用 resolve_translation(raw_translations[en], context_tags) 获取翻译
       - 如果 raw_translations 为 None 或该条目无 contexts，使用 translations[en]（现有行为）
       - stats 新增 "contextual" 字段，统计使用上下文翻译的次数

       关键实现细节：
       - 对于 long 字符串：content.count(en) 检查后，逐个位置替换（不能再全局 replace）
         需要改为 finditer 找到所有位置，逆序遍历，每个位置独立判断上下文
       - 对于 medium/short 字符串：已经有 finditer，只需在每个 match 上增加上下文检测
       - raw_translations 为 None 或 context_index 为 None 时，完全走原有逻辑路径

    3. 修改 apply.py 的 cmd_apply：
       - 从 map_data 中获取 raw_translations（Plan 01 新增的返回字段）
       - 读取 cli.js 内容后，调用 build_context_index(content) 构建索引
       - 将 raw_translations 和 context_index 传入 apply_translations
       ```python
       # 在 cmd_apply 中新增
       from scripts.i18n.core.context_detector import build_context_index

       raw_translations = map_data.get("raw_translations", {})
       context_index = build_context_index(content) if raw_translations else None

       modified, stats = apply_translations(
           content, translations, skip_words,
           raw_translations=raw_translations,
           context_index=context_index,
       )
       ```

       注意：build_context_index 只在有 raw_translations（即可能有上下文条目）时才调用，
       避免对没有上下文条目的映射表增加不必要的扫描开销。

    4. 确保现有 test_replacer.py 中的测试不回归：
       - 所有现有测试不传 raw_translations 和 context_index，走原有路径
  </action>
  <verify>
    <automated>cd /Users/zhaolulu/Projects/claude-code-i18n && python -m pytest tests/unit/test_replacer.py -x -v</automated>
  </verify>
  <done>
    - apply_translations 支持 raw_translations 和 context_index 参数
    - 替换时根据位置上下文优先选择精确翻译
    - 无上下文参数时行为完全不变
    - stats 包含 "contextual" 计数
    - apply 命令集成上下文检测流程
    - 现有测试全部通过
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/zhaolulu/Projects/claude-code-i18n
python -m pytest tests/unit/test_replacer.py -x -v
python -m pytest tests/unit/test_apply_cmd.py -x -v
```
</verification>

<success_criteria>
1. apply_translations 在有上下文信息时按位置选择翻译
2. 无上下文信息时行为与 v3.0 完全一致（向后兼容）
3. 精确上下文匹配优先于全局默认
4. apply 命令流程集成上下文检测
5. 全部测试通过（新测试 + 现有测试不回归）
</success_criteria>

<output>
After completion, create `.planning/phases/04-context-aware-arch/04-02-SUMMARY.md`
</output>
