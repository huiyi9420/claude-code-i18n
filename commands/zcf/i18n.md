---
description: 'Claude Code CLI 汉化工具（一键应用/更新/恢复）'
allowed-tools: Bash, Read, Write, Edit, Glob, Grep
---

$ARGUMENTS

## Claude Code CLI 汉化工具

你是 Claude Code CLI 的中文汉化助手。根据参数执行操作，**无参数时执行一键汉化**。

### 路径约定

| 项目 | 路径 |
|------|------|
| 汉化引擎 | `~/.claude/scripts/i18n/localize.py` |
| 翻译映射表 | `~/.claude/scripts/i18n/zh-CN.json` |
| 跳过词表 | `~/.claude/scripts/i18n/skip-words.json` |
| 项目仓库 | `/Users/zhaolulu/Projects/claude-code-i18n` |

### CLI 安装路径检测

```bash
# Volta 安装
cli_dir=$(dirname "$(find ~/.volta/tools/image/packages/@anthropic-ai/claude-code -name "cli.js" -path "*/lib/node_modules/*" 2>/dev/null | head -1)")
# npm 全局安装
[ -z "$cli_dir" ] && cli_dir=$(dirname "$(find /usr/local/lib/node_modules/@anthropic-ai/claude-code -name "cli.js" 2>/dev/null | head -1)")
```

### 参数 → 行为映射

| 参数 | 行为 |
|------|------|
| 无 / apply | 一键汉化（检测版本→提取→翻译→应用→验证） |
| restore | 恢复英文原文 |
| status | 查看汉化状态 |

---

### 一键汉化流程（无参数 / apply）

完整执行以下步骤，**中间遇到错误立即停止并告知用户**：

#### 步骤 0：版本变更检测（关键！）

```bash
# 获取当前 CLI 版本
current_version=$(claude --version 2>/dev/null | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)

# 获取映射表记录的版本
lang_version=$(python3 -c "import json; print(json.load(open('$HOME/.claude/scripts/i18n/zh-CN.json')).get('_meta',{}).get('cli_version',''))" 2>/dev/null)

# 检查是否有备份
backup_exists=$(test -f "$cli_dir/cli.bak.en.js" && echo "yes" || echo "no")
```

**版本变更处理逻辑：**

1. **版本相同 + 已有备份** → 跳到步骤 2（正常汉化）
2. **版本不同 + 有旧备份** → 删除旧备份，重新汉化
   ```bash
   rm -f "$cli_dir/cli.bak.en.js"
   ```
3. **版本不同 + 无备份** → 直接从新版本创建备份
4. **版本相同 + 无备份** → 正常汉化（apply 会自动创建备份）

版本变更时告知用户："检测到 CLI 版本从 {旧版本} 更新到 {新版本}，将重新汉化"

更新映射表的 `_meta.cli_version` 为当前版本。

#### 步骤 1：获取当前状态

```bash
python3 ~/.claude/scripts/i18n/localize.py status
python3 ~/.claude/scripts/i18n/localize.py version
```

记录 `version` 和 `localized` 状态。

#### 步骤 2：提取新字符串

```bash
python3 ~/.claude/scripts/i18n/localize.py extract
```

解析输出 JSON，取 `strong` 数组中的字符串。

#### 步骤 3：AI 翻译

对提取出的新字符串（`strong` 列表），逐条翻译为中文：

**翻译原则：**
- 简洁、符合中文习惯
- 保留变量占位符如 `{0}`、`%s`、`${var}` 不翻译
- 保留命令名如 `/login`、`/config` 不翻译
- 保留专有名词如 `MCP`、`API`、`Git` 不翻译
- 技术术语保留英文或用通用中文译法
- **短字符串（<10字符）需谨慎，只在明确是 UI 文本时才翻译**
- **加载动词列表**（如 Marinating、Pondering、Sock-hopping 等）翻译为"XX中"格式
- **React 拼接字符串**（如 `"Claude Code","'","ll be able to..."`) 需特别关注，翻译被拆分的部分

**跳过规则：** 遇到以下情况不翻译，记入 skip-words.json：
- 看起来是代码标识符/类名/方法名
- 是第三方库的内部日志
- 是协议/标准的技术描述
- 不确定是否为用户可见的 UI 文本
- 代码逻辑状态值（如 `status: "running"`、`"completed"`、`"failed"`）

**特别注意：不可翻译的状态值**
以下字符串是 JavaScript 状态机的逻辑判断值，**绝对不能翻译**，否则会破坏功能：
- `"running"`、`"pending"`、`"completed"`、`"failed"`、`"killed"`、`"idle"`、`"starting"`、`"stopped"`
- `"in_process_teammate"`、`"local_bash"`、`"local_agent"`、`"remote_agent"`、`"dream"`、`"local_workflow"`、`"monitor_mcp"`
- `"leader"`、`"foreground"`（作为 action 值时）

#### 步骤 4：更新映射表

将新翻译合并到 `~/.claude/scripts/i18n/zh-CN.json`：
- 读取现有 JSON
- 在 `translations` 对象末尾追加新条目
- 更新 `_meta.cli_version` 为当前版本
- 更新 `_meta.version` 递增
- 写入文件

将跳过的字符串记入 `~/.claude/scripts/i18n/skip-words.json` 的 `skip` 数组。

#### 步骤 5：应用汉化

```bash
python3 ~/.claude/scripts/i18n/localize.py apply
```

解析输出的 JSON，确认 `ok: true`。

#### 步骤 6：Hook UI 消息汉化

CLI 源码中有部分 hook 相关的显示字符串是**模板拼接**（如 `hookName + " says: " + content`），无法通过翻译映射表替换，需要直接在 `cli.js` 中进行 sed 替换。

定位 `cli.js` 路径：
```bash
cli_js=$(python3 ~/.claude/scripts/i18n/localize.py path 2>/dev/null)
[ -z "$cli_js" ] && cli_js=$(find ~/.volta/tools/image/packages/@anthropic-ai/claude-code -name "cli.js" -path "*/lib/node_modules/*" 2>/dev/null | head -1)
```

依次执行以下 sed 替换（注意 macOS 需要用 `sed -i ''`）：

```bash
# 1. hook 系统消息: "{hookName} says: {content}" → "{hookName} 说道：{content}"
sed -i '' 's/" says: "/" 说道："/g' "$cli_js"

# 2. hook 阻塞错误
sed -i '' 's/hook returned blocking error/hook 返回了阻塞错误/g' "$cli_js"

# 3. hook 非阻塞错误
sed -i '' 's/" hook error"/" hook 错误"/g' "$cli_js"

# 4. hook 警告
sed -i '' 's/" hook warning"/" hook 警告"/g' "$cli_js"

# 5. hook 停止继续
sed -i '' 's/hook stopped continuation: /hook 停止了继续执行：/g' "$cli_js"

# 6. hook 推迟工具
sed -i '' 's/" deferred "/" 推迟了 "/g' "$cli_js"
sed -i '' 's/· resume with -p --resume to continue/· 使用 -p --resume 恢复继续/g' "$cli_js"

# 7. hook 权限决策
sed -i '' 's/"Allowed by "/"已允许（由 "/g' "$cli_js"
sed -i '' 's/"Denied by "/"已拒绝（由 "/g' "$cli_js"

# 8. 异步钩子
sed -i '' 's/"Async hook "/"异步钩子 "/g' "$cli_js"
sed -i '' 's/" completed"/" 已完成"/g' "$cli_js"

# 9. hook 运行统计
sed -i '' 's/"Ran "/"运行了 "/g' "$cli_js"
```

**注意事项：**
- 这些替换字符串可能在非 hook 上下文中也有出现，替换后需通过步骤 7 的语法验证确认无副作用
- Hook 事件类型名（`PreToolUse`、`PostToolUse`、`SessionStart` 等）保留英文
- 每个 CLI 版本更新后需要重新执行此步骤

#### 步骤 7：语法验证

```bash
node --check "$cli_js"
```

如果验证失败，立即恢复：
```bash
python3 ~/.claude/scripts/i18n/localize.py restore
```

并在恢复后告知用户 Hook UI 汉化步骤可能需要调整。

#### 步骤 8：功能验证

```bash
claude -p "回复两个字：成功" 2>&1
```

确认输出包含"成功"且无报错。

#### 步骤 9：汇报结果

向用户展示：
- 当前 CLI 版本
- 版本变更（如有）
- 新提取的字符串数量
- 新翻译数量 / 跳过数量
- 总替换处数
- 语法验证结果
- 功能测试结果
- 提醒：请重启 Claude Code 查看效果

#### 步骤 10：可选 — 汉化插件与命令描述

CLI 汉化完成后，使用 AskUserQuestion 询问用户：

**问题：** "是否同时汉化插件和命令的描述？（GSD 命令 + everything-claude-code 技能等）"

如果用户选择是，执行以下子流程：

##### 10.1 汉化 GSD 命令描述

扫描 `~/.claude/commands/gsd/*.md`，找到 frontmatter 中 `description` 字段为英文的文件，逐个翻译为中文。
只修改 `description:` 行，不改其他内容。

##### 10.2 汉化 everything-claude-code 技能描述

扫描插件缓存中 `~/.claude/plugins/cache/everything-claude-code/everything-claude-code/*/agents/skills/*/SKILL.md`，找到 `description` 为英文的文件，逐个翻译。
只修改 `description:` 行。

##### 10.3 汉化其他英文技能描述

扫描 `~/.claude/skills/*/SKILL.md`，找到 `description` 为英文的文件，翻译。

##### 10.4 汇报扩展汉化结果

展示各部分汉化数量。

#### 步骤 11：可选 — 同步到远程仓库

使用 AskUserQuestion 询问用户：

**问题：** "是否同步更新到 GitHub 远程仓库？"

如果用户选择是：
1. 将 `~/.claude/scripts/i18n/` 下的 `zh-CN.json` 和 `skip-words.json` 复制到 `/Users/zhaolulu/Projects/claude-code-i18n/scripts/`
2. 在仓库目录执行 `git add` → `git commit` → `git push origin main`
3. commit 消息格式：`feat: update zh-CN translations for CLI v{版本}`

---

### restore 流程

```bash
python3 ~/.claude/scripts/i18n/localize.py restore
```

确认恢复成功后告知用户。

---

### status 流程

```bash
python3 ~/.claude/scripts/i18n/localize.py status
```

以表格形式展示：
- CLI 版本
- 汉化状态（已汉化/未汉化）
- 映射表条目数
- 映射表对应的 CLI 版本
- 备份文件状态

---

### 故障排除

| 问题 | 解决方案 |
|------|---------|
| `extract` 后翻译表条目减少 | 这是 bug。立即从 git 恢复：`cd /Users/zhaolulu/Projects/claude-code-i18n && git checkout HEAD -- scripts/zh-CN.json` |
| CLI 更新后汉化失效 | 运行 `/zcf:i18n`，步骤 0 会自动检测版本变更并重新汉化 |
| 语法验证失败 | 自动回滚，检查 Hook sed 替换是否命中非目标文本 |
| 找不到 cli.js | 检查 Volta/npm 安装路径是否变化 |
